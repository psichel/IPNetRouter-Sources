// =================================================================================//	CRoutesWindow.h	 	©1996 Sustainable Softworks. All rights reserved.// =================================================================================//	Configure Routes Window#pragma once#include "Cwindow.h"#include <LListener.h>#include "CRoutesTable.h"class LCaption;class LEditField;class CRoutesWindow : public CWindow, public LListener {public:	enum { class_ID = 'CRTw' };								CRoutesWindow( LStream *inStream );	virtual		 				~CRoutesWindow();	void				FinishCreateSelf();	Boolean				HandleKeyPress(const EventRecord&	inKeyEvent);	virtual void		ListenToMessage( MessageT inMessage, void *ioParam );	virtual Boolean		ObeyCommand(								CommandT			inCommand,								void				*ioParam);	void				UpdateTable();	void				DisplayRouteEntry(RouteEntry_t* inRouteEntry);		void				IoctlResponse(long inResult);protected:	private:		CRoutesTable*		mRoutesTable;	LStdPopupMenu*		mRouteTypePopup;	// pointer to popup menu object	SInt16				mRouteTypePopupMax;	// ioctl	LCaption*			mStatusCaption;		// pointer to status caption object};