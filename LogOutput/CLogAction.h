// =================================================================================//	CLogAction.h	 	©1996-99 Sustainable Softworks. All rights reserved.// =================================================================================//	Logging module for capturing router status messages//	This module formats log messages and writes them to the CLogData object.//	The CLogWindow displays the contents of the CLogData when open.#pragma once#include <OpenTransport.h>#include <PP_Networking.h>class CLogAction {public:						CLogAction();	virtual		 		~CLogAction();	void		LogText(ConstStr255Param inStr, ResultT inResult);	void		LogCText(char* inStr);	void		LogText(ConstStr255Param inStr);	void		Update();protected:	void		LogError(ResultT inResult);private:	Handle			mFormatH;		// Time/Date formatting resource	Boolean			mPending;		// update is pending};