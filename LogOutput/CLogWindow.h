// =================================================================================//	CLogWindow.h	 		©1996 Sustainable Softworks. All rights reserved.// =================================================================================//	Logging window for displaying router status messages#pragma once#include "CWindow.h"#include <LTextEditView.h>//#include <OpenTransport.h>//#include <PP_Networking.h>class CLogWindow : public CWindow, public LListener {public:	enum { class_ID = 'LWin' };							CLogWindow( LStream *inStream );	virtual		 			~CLogWindow();	void			FinishCreateSelf();	virtual void	ListenToMessage( MessageT inMessage, void *ioParam );	void			AttemptClose();	Boolean			HandleKeyPress(const EventRecord&	inKeyEvent);	virtual Boolean	ObeyCommand(						CommandT			inCommand,						void				*ioParam);	virtual void	FindCommandStatus(CommandT inCommand,						Boolean &outEnabled, Boolean &outUsesMark,						UInt16 &outMark, Str255 outName);	void			DoPrint();private:	LTextEditView	*mTextEdit;		// the TextEdit pane	TEHandle		mTextEditH;		// the TE handle};const SInt16 kScrollerPane				= 1001;const SInt16 kTextEditPane				= 1002;