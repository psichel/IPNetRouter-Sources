// ===========================================================================//	CGURLTask.h	 			©1996 Sustainable Softworks. All rights reserved.// ===========================================================================//	Class that steps through handling a GURL Apple Event asynchronously#pragma once#include <LPeriodical.h>class CGURLTask :	public LPeriodical {public:						CGURLTask(LCommander *inSuperCommander);	virtual				~CGURLTask();	Boolean				Start(ConstStr255Param inURL);	void				Abort();	virtual void		SpendTime(const EventRecord &inMacEvent);protected:	LCommander*			mSuperCommander;	UInt8				mStepNumber;	// keep track of what step we're on	UInt32				mStartTime;		// used to measure delay since start	LWindow*			mURLWindow;		// the window to invoke	UInt8				mURLAction;		// the action to invoke	LStr255				mURLString;		// the URL string	Boolean				mGURLPending;};const UInt8 kURLnone					= 0;const UInt8 kURLtraceroute				= 1;const UInt8 kURLtracerouteAddress		= 2;const UInt8 kURLping					= 3;const UInt8 kURLpingAddress				= 4;const UInt8 kURLwhois					= 5;const UInt8 kURLwhoisAddress			= 6;const UInt8 kURLfinger					= 7;const UInt8 kURLfingerAddress			= 8;const UInt8 kURLnslookup				= 9;const UInt8 kURLnslookupAddress			= 10;