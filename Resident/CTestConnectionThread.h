// ===========================================================================//	CTestConnectionThread.h ©1998 Sustainable Softworks. All rights reserved.// ===========================================================================//	Thread object to test if reconnection is needed, that is,//	there is traffic for an on demand interface#pragma once#include <LThread.h>class CResidentIfInfo;class CTestConnectionThread : 		public LThread {public:						CTestConnectionThread(								CResidentIfInfo* inResInfo);	virtual				~CTestConnectionThread();	virtual void		Abort();	virtual void		Pause(UInt32 inPauseSeconds);	protected:	virtual void*		Run();	CResidentIfInfo*	mResInfo;	Boolean				mAbort;	UInt32				mPauseSeconds;	Boolean				mPauseRepeat;	OSStatus			mResult;};