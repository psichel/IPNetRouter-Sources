// ===========================================================================//	CTickleConnectionThread.h ©1998 Sustainable Softworks. All rights reserved.// ===========================================================================//	Thread object to tickle connection every few minutes//	so it doesn't drop from inactivity#pragma once#include <LThread.h>class CResidentIfInfo;class CTurboUDPEndpoint;class CSendUDPThread;class CTickleConnectionThread : 		public LThread {public:						CTickleConnectionThread(								CResidentIfInfo* inResInfo);	virtual				~CTickleConnectionThread();	virtual void		Abort();	virtual void		ProviderIsClosed();	virtual void		SetupPing();	virtual void		SendPing();	protected:	virtual void*		Run();	CResidentIfInfo*	mResInfo;	Boolean				mAbort;	CTurboUDPEndpoint*	mEndpoint;	UInt32				mPingAddress;	UInt32				mPingCounter;	OSStatus			mResult;};