/* 	StringUtilities.cpp: Routines to convert addresses to strings    	   	This document contains proprietary information developed by    		FCR Software Inc.   		222 Third Street, Suite 3130   		Cambridge, MA 02142   		   	and should not be distributed, copied, or disclosed without explicit   	permission of FCR Software, Inc..   	   	You may use this document and the source herein for developing    	applications that use the LinkUPPP! (FCR PPP) driver.	Copyright 1997-98 FCR Software, Inc.		November 6, 1997 - Jessica Kahn <jess@fcr.com>*/#include <stdio.h>#include "StringUtilities.h"// ---------------------------------------------------------------------------//		¥ IPtoString() 	//			Takes an IP address (long), and makes it a c-string// ---------------------------------------------------------------------------void IPtoString(long ipAddr, char * text) {	unsigned char b1, b2, b3, b4;		b1 = ipAddr >> 24;	b2 = ipAddr >> 16;	b3 = ipAddr >> 8;	b4 = ipAddr;		sprintf(text, "%d.%d.%d.%d", b1, b2, b3, b4);}	// ---------------------------------------------------------------------------//		¥ ATtoString() 	//			Takes an AppleTalk address (long), and makes it a c-string// ---------------------------------------------------------------------------void ATtoString(long atAddr, char * text) {	unsigned short b1;	unsigned char b2;	b1 = atAddr >> 8;	b2 = atAddr;		sprintf(text, "%d.%d", b1, b2);}