// =================================================================================//	CFilterWindow.h	 	©1998 Sustainable Softworks. All rights reserved.// =================================================================================//	IP Filter Window#pragma once#include "Cwindow.h"#include "MyTypes.h"#include <LListener.h>class LCaption;class LEditField;class CFilterTable;class CIPNumberEditField;class CFilterWindow : public CWindow, public LListener {public:	enum { class_ID = 'FilW' };								CFilterWindow( LStream *inStream );	virtual		 				~CFilterWindow();	void				FinishCreateSelf();	//void				UpdateMenu();	//void				ActivateSelf();	Boolean				HandleKeyPress(const EventRecord&	inKeyEvent);	virtual void		ListenToMessage( MessageT inMessage, void *ioParam );	virtual Boolean		ObeyCommand(								CommandT			inCommand,								void				*ioParam);		void				UpdateTable();	void				DisplayFilterEntry(FilterEntry_t*	inFilterEntry);	void				DoCommand(MessageT inEventCode);	void				IoctlResponse(long inResult);protected:	LStdPopupMenu*		mFilterPortPopup;	LStdPopupMenu*		mDirectionPopup;	LStdPopupMenu*		mActionPopup;	LStdPopupMenu*		mProtocolPopup;	LStdPopupMenu*		mAckFlagPopup;	LStdPopupMenu*		mProtocolPortNamesPopup;	LStdPopupMenu*		mIcmpTypesPopup;	CIPNumberEditField*	mSourceNetField;	LEditField*			mSourcePortsField;	CIPNumberEditField*	mDestNetField;	LEditField*			mDestPortsField;	private:		CFilterTable*		mFilterTable;	LCaption*			mStatusCaption;		// pointer to status caption object};// constantsconst Str31		kPortNameAllStr				= "\pAll";