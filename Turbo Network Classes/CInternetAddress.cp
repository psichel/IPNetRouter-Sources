// ===========================================================================//	CInternetAddress.cp		©1996 Metrowerks Inc. All rights reserved.// ===========================================================================// This class represents both IP and DNS style Internet addresses. It will// automatically map between DNS style and IP style addresses for you as necessary.#include "CInternetAddress.h"#include <LInternetMapper.h>#include <UNetworkFactory.h>#include <OpenTransport.h>#include <OpenTptInternet.h>// ---------------------------------------------------------------------------//		¥ CInternetAddress// ---------------------------------------------------------------------------//	ConstructorCInternetAddress::CInternetAddress(	UInt32					inHostAddress,	UInt16					inHostPort)	: LInternetAddress(inHostAddress, inHostPort){}// ---------------------------------------------------------------------------//		¥ CInternetAddress// ---------------------------------------------------------------------------//	ConstructorCInternetAddress::CInternetAddress(	ConstStringPtr			inHostAddress,	UInt16					inHostPort,	Boolean					inLookupNow)	: LInternetAddress(inHostAddress, inHostPort, inLookupNow){}// ---------------------------------------------------------------------------//		¥ CInternetAddress// ---------------------------------------------------------------------------//	Alternate Constructor... assumes you don't want to use an addressCInternetAddress::CInternetAddress(	UInt16	inHostPort)	: LInternetAddress(inHostPort){}// ---------------------------------------------------------------------------//		¥ CInternetAddress// ---------------------------------------------------------------------------//	Copy constructorCInternetAddress::CInternetAddress(	const CInternetAddress& inOriginal)	: LInternetAddress(inOriginal){}// ---------------------------------------------------------------------------//		¥ ~CInternetAddress// ---------------------------------------------------------------------------//	DestructorCInternetAddress::~CInternetAddress(){}