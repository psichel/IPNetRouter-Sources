// ===========================================================================//	CInternetAddress.h		©1996 Metrowerks Inc. All rights reserved.// ===========================================================================//	LInternetAddress that includes LRMPObject as a mix-in#ifndef _H_CInternetAddress#define _H_CInternetAddress#pragma once#if defined(__CFM68K__) && !defined(__USING_STATIC_LIBS__)	#pragma import on#endif#include <LReentrantMemoryPool.h>#include <LInternetAddress.h>// ===========================================================================//		¥ CInternetAddress// ===========================================================================//	Wrapper for the IP addresses that are used internally by the//	Internet to describe another computer's location. DNS names//	(like "ftp.metrowerks.com") must be mapped to IP addresses (127.0.0.1)//	before connections are established. This object will handle such//	conversions automatically.class CInternetAddress : public LInternetAddress, public LRMPObject {public:								CInternetAddress(										UInt32					inHostAddress = 0,										UInt16					inHostPort = 0);								CInternetAddress(										ConstStringPtr			inHostAddress,										UInt16					inHostPort,										Boolean					inLookupNow);								CInternetAddress(										UInt16					inHostPort);								CInternetAddress(										const CInternetAddress& inOriginal);	virtual						~CInternetAddress();protected:private:};#if defined(__CFM68K__) && !defined(__USING_STATIC_LIBS__)	#pragma import reset#endif#endif