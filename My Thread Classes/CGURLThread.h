// ===========================================================================//	CGURLThread.h			©1997 Sustainable Softworks. All rights reserved.// ===========================================================================//	Thread object to step through application startup sequence#pragma once#include <LThread.h>class CIPRouterApp;class CGURLThread : 		public LThread {public:						CGURLThread(								CIPRouterApp*		inApp,								ConstStr255Param	inURL);	virtual				~CGURLThread();	protected:	virtual void*		Run();	CIPRouterApp* 		mApp;	LStr255				mURLString;		OSStatus			mResult;};const UInt8 kURLnone					= 0;const UInt8 kURLtraceroute				= 1;const UInt8 kURLtracerouteAddress		= 2;const UInt8 kURLping					= 3;const UInt8 kURLpingAddress				= 4;const UInt8 kURLwhois					= 5;const UInt8 kURLwhoisAddress			= 6;const UInt8 kURLfinger					= 7;const UInt8 kURLfingerAddress			= 8;const UInt8 kURLnslookup				= 9;const UInt8 kURLnslookupAddress			= 10;const UInt8 kURLscan					= 11;const UInt8 kURLscanAddress				= 12;