// ===========================================================================//	CAuthorizationThread.h		©1999 Sustainable Softworks. All rights reserved.// ===========================================================================//	Thread object to step through application Authorization sequence#pragma once#include <LThread.h>class CIPRouterApp;class CAuthorizationThread : 		public LThread {public:						CAuthorizationThread(								CIPRouterApp*		inApp);	virtual				~CAuthorizationThread();	protected:	virtual void*		Run();	UInt32				FindFirstEthernet();	Boolean				GetParentDomain(Str255 inStr, Str255 outStr);	CIPRouterApp* 		mApp;		OSStatus			mResult;};